spring:
  application:
    name: Config-Defoult
eureka:
  client:
    fetch_registry: true
    register-with-eureka: true
    service-url:
      defaultzone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
